import { useState } from "react";

type FeedbackItem = {
  id: number;
  author: string;
  message: string;
};

type FeedbackFormProps = {
  // fill here
};

function FeedbackForm(
  props: FeedbackFormProps
) {
  return (
    <form>
      {/* fill here */}
    </form>
  );
}

type SearchInputProps = {
  // fill here
};

function SearchInput(props: SearchInputProps) {
  return (
    <section>
      {/* fill here */}
    </section>
  );
}

type FeedbackListProps = {
  // fill here
};

function FeedbackList(props: FeedbackListProps) {
  return (
    <section>
      {/* fill here */}
    </section>
  );
}

function App() {
  return (
    <main>
      {/* create feedback state, search state, and derived filtered feedback */}
    </main>
  );
}

export default App;
